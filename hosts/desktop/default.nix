{ config, lib, ... }:

{
  # Desktop-specific overrides
  # Example overrides (uncomment and modify as needed):
  # core.terminal.command = "alacritty";
  # windowManager.gaps.inner = 4;
  # windowManager.gaps.outer = 8;
}
