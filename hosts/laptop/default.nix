{ config, lib, ... }:

{
  # Laptop-specific overrides
  # Example overrides (uncomment and modify as needed):
  # core.terminal.command = "foot";
  # windowManager.gaps.inner = 1;
  # windowManager.gaps.outer = 3;
}
